import {FormHelperText} from '@material-ui/core';
import styled from 'styled-components';

export const StyledFormTextHelper = styled(FormHelperText)`
  margin: 0 0 5px;
  font-family: Roboto, Oxygen, Ubuntu, cursive;
  font-size: 13px;
`;
